# 🚀 Quick Start: Улучшенная AI-команда

## За 3 минуты до первого проекта

### Шаг 1: Получите бесплатный API ключ (выберите один)

**Вариант A: DeepSeek** (Рекомендуется - лучшее качество кода)
```bash
# 1. Откройте: https://platform.deepseek.com/api_keys
# 2. Зарегистрируйтесь (email + пароль)
# 3. Создайте API key
# 4. Установите:
export LLM_MODE=deepseek
export DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

**Вариант B: GitHub Models** (GPT-4o/Claude бесплатно!)
```bash
# 1. Откройте: https://github.com/settings/tokens
# 2. Create classic token (права: read:packages)
# 3. Установите:
export LLM_MODE=github
export GITHUB_MODEL=gpt-4o
export GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxx
```

**Вариант C: Cerebras** (Самый быстрый в мире)
```bash
# 1. Откройте: https://cloud.cerebras.ai/
# 2. Sign up
# 3. Получите API key
# 4. Установите:
export LLM_MODE=cerebras
export CEREBRAS_API_KEY=csk-xxxxxxxxxxxxxxxx
```

### Шаг 2: Установите зависимости

```bash
cd backend
pip install -r requirements-backend.txt
```

### Шаг 3: Запустите

```bash
cd ..
make dev
```

Откроется:
- Backend: http://localhost:8000
- Frontend: http://localhost:3000

### Шаг 4: Создайте проект

```bash
curl -X POST http://localhost:8000/api/projects \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Snake Game 3D",
    "description": "3D snake game using Three.js with scoring and levels",
    "target": "web"
  }'
```

Или через UI: http://localhost:3000

---

## 🎯 Что изменилось

### Было (старая версия):
- ❌ Только Groq (платный)
- ❌ Базовые промпты
- ❌ Нет тестирования
- ❌ Код не форматируется
- ❌ Developer не видит контекст

### Стало (новая версия):
- ✅ 3 бесплатных провайдера (DeepSeek, GitHub, Cerebras)
- ✅ Промпты с best practices (SOLID, DRY, Security)
- ✅ Автоматическое тестирование (4 типа проверок)
- ✅ Автоформатирование всего кода
- ✅ Developer читает существующие файлы
- ✅ Reviewer стал в 3x строже

---

## 📊 Сравнение моделей

| Модель | Качество | Скорость | Лимит | Рекомендация |
|--------|----------|----------|-------|--------------|
| **DeepSeek V3** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 10M/день | 🏆 Лучший выбор |
| **GPT-4o** (GitHub) | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | Щедрый | Для топ качества |
| **Claude 3.5** (GitHub) | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | Щедрый | Для сложных задач |
| **Cerebras Llama** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Щедрый | Для скорости |
| **Groq** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | Ограничен | Если уже есть ключ |

---

## 🔥 Примеры команд

### Переключение модели на лету
```bash
# В .env файле или экспорте:
export LLM_MODE=deepseek      # Для качества
export LLM_MODE=github        # Для GPT-4o/Claude
export LLM_MODE=cerebras      # Для скорости
export LLM_MODE=mock          # Для тестов без API
```

### Увеличить параллелизм
```bash
export LLM_SEMAPHORE=20  # Больше одновременных запросов
```

### Посмотреть логи
```bash
# Backend логи
tail -f backend/logs/app.log

# WebSocket в браузере (открой DevTools → Network → WS)
```

---

## ✅ Чек-лист: Проверь что работает

После запуска проекта проверь логи:

```
✅ [CEO] Creating 6 quality steps...
✅ [Developer] Reading project context (5 files)...
✅ [Developer] Generating high-quality code with best practices...
✅ [Reviewer] Review complete. Score: 85/100
✅ [Formatter] Formatted 8 files
✅ [Tester] Running comprehensive testing...
✅ [Tester] ✅ All tests PASSED!
```

Если видишь все эти сообщения - **всё работает идеально!** 🎉

---

## 🐛 Частые ошибки

### Ошибка: "API key not found"
```bash
# Проверь что переменная установлена:
echo $DEEPSEEK_API_KEY
echo $GITHUB_TOKEN
echo $CEREBRAS_API_KEY

# Если пусто - экспортируй заново
```

### Ошибка: "Cannot import openai"
```bash
# Установи зависимости:
cd backend
pip install openai>=1.12.0
```

### Проект создается но код плохой
```bash
# Попробуй другую модель:
export LLM_MODE=github
export GITHUB_MODEL=gpt-4o
```

---

## 💡 Pro Tips

1. **DeepSeek V3** лучше всего для кода - используй его по умолчанию
2. **Claude 3.5** (через GitHub) лучше для сложной логики и архитектуры
3. **Cerebras** для быстрого прототипирования (2000+ tok/s)
4. Reviewer автоматически проверяет критические файлы (index.html, main.js, app.py)
5. Testing Agent найдет большинство багов до запуска

---

## 📚 Полная документация

- [IMPROVEMENTS.md](./IMPROVEMENTS.md) - Детальное описание всех улучшений
- [README.md](./README.md) - Основная документация
- Backend API: http://localhost:8000/docs (FastAPI Swagger)

---

**Готово! Создавай качественные проекты с AI-командой мирового класса! 🚀**

